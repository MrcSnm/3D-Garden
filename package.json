{
  "name": "sett",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "dev": "esbuild source/main.js --bundle --minify --sourcemap --outfile=build/app.js --servedir=build",
    "build": "esbuild source/main.js --bundle --minify --outfile=build/app.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "gsap": "^3.13.0",
    "howler": "^2.2.4",
    "three": "^0.177.0"
  },
  "devDependencies": {
    "esbuild": "^0.25.5"
  }
}
